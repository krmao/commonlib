kind: pipeline
type: docker
name: build-iot-new

steps:
  - name: test
    image: maven:3-jdk-10
    commands:
      - echo 'testtest'
      - cd  ./simple-pom
      - mvn update
      - mvn clean install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
trigger:
  branch:
    - master
  event:
    - push

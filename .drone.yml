kind: pipeline
type: docker
name: build-iot-new

steps:
  - name: test
    image: maven:latest
    commands:
      - echo 'testtest'
      - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B

